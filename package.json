{
  "name": "fashion-store",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo: Next.js storefront + NestJS API for fashion e-commerce",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend & npm run start:dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run start:dev -w backend",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w frontend && npm run lint -w backend"
  }
}
